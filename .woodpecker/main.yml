clone:
  git:
    image: techknowlogick/drone-git:latest

pipeline:
  publish:
    image: plugins/kaniko
    repo: foo/bar
    dry_run: true
    tags: latest
    username: foo
    password: bar
    dockerfile: Dockerfile
